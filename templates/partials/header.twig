<nav class="navbar navbar-expand-lg fixed-top navbar-transparent" color-on-scroll="60">
    <div class="container">
        <div class="navbar-translate">
            <a class="navbar-brand brand" href="/">
                <span class="brand__mark">RES</span>
                <span class="brand__text">
                    <strong>{{ site.brand_name|default('Çözüm Oto Elektrik') }}</strong>
                    <small>Renault Elektronik Servisi</small>
                </span>
            </a>
            <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navigation"
                    aria-controls="navigation" aria-expanded="false" aria-label="Menüyü aç/kapat">
                <span class="navbar-toggler-bar bar1"></span>
                <span class="navbar-toggler-bar bar2"></span>
                <span class="navbar-toggler-bar bar3"></span>
            </button>
        </div>

        <div class="collapse navbar-collapse justify-content-end" id="navigation">
            <ul class="navbar-nav">
                <li class="nav-item"><a class="nav-link" href="/">Anasayfa</a></li>
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" href="/hizmetler" id="nav-services" data-toggle="dropdown"
                       aria-haspopup="true" aria-expanded="false">Hizmetler</a>
                    <div class="dropdown-menu dropdown-menu-right" aria-labelledby="nav-services">
                        <a class="dropdown-item dropdown-item--all" href="/hizmetler">Tüm Hizmetler</a>
                        <div class="dropdown-divider"></div>
                        {% for s in menu_services %}
                            <a class="dropdown-item" href="/hizmet/{{ s.slug }}">{{ s.title }}</a>
                        {% endfor %}
                    </div>
                </li>
                <li class="nav-item"><a class="nav-link" href="/hakkimizda">Hakkımızda</a></li>
                <li class="nav-item"><a class="nav-link" href="/galeri">Galeri</a></li>
                <li class="nav-item"><a class="nav-link" href="/iletisim">İletişim</a></li>
                {% if site.phone_primary %}
                <li class="nav-item d-lg-none">
                    <a class="nav-link" href="{{ tel_link(site.phone_primary) }}"><i class="fa fa-phone"></i> {{ site.phone_primary }}</a>
                </li>
                {% endif %}
                {% if site.whatsapp %}
                <li class="nav-item">
                    <a class="btn btn-success btn-round nav-wa" href="{{ whatsapp_link('Merhaba, bilgi almak istiyorum.') }}"
                       target="_blank" rel="noopener"><i class="fa fa-whatsapp"></i> WhatsApp</a>
                </li>
                {% endif %}
            </ul>
        </div>
    </div>
</nav>
